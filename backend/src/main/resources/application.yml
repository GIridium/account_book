server:
  port: 8080
  error:
    include-stacktrace: always
    include-message: always

spring:
  application:
    name: account-book-backend
  datasource:
    url: jdbc:postgresql://localhost:5432/account_book
    username: postgres
    password: JIANG060504
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect
        jdbc:
          time_zone: UTC
  flyway:
    enabled: true
    baseline-on-migrate: true
    validate-on-migrate: false
